{
  "mcpServers": {
    "mem": {
      "command": "<BASEMEM_ROOT>/venv/bin/python3",
      "args": ["<BASEMEM_ROOT>/mem-mcp.py"],
      "env": {
        "BASEMEM_DB_PATH": "$HOME/.basemem/basemem.db"
      }
    }
  }
}
